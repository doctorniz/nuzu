scalar DateTime

scalar JSON

type ErrorType {
    error: Boolean
    message: String
}

type Pagination {
    page: Int
    offset: Int
}

type Mutation {
    LogInUser(username: String, email: String, password: String): ErrorType
}